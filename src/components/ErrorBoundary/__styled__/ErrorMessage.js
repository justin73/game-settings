import styled from "styled-components"

export const ErrorMsgWrapper = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  height: 10vh;
  color: red;
  h4 {
    font-weight: 600;
    opacity: 0.5;
  }
  svg {
    margin: 16px;
  }
`
