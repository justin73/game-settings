export const mockSchema = {
  id: "123",
  title: "Difficulty",
  description: "A simple form example.",
  type: "object",
  properties: {
    level: {
      type: "string",
      enum: ["easy", "hard"]
    },
    maxEnemyCount: {
      type: "number"
    }
  }
}

export const mockData = {
  id: "123",
  title: "Difficulty",
  description: "A simple form example.",
  type: "object",
  properties: {
    level: {
      type: "string",
      enum: ["easy", "hard"]
    },
    maxEnemyCount: {
      type: "number"
    }
  }
}
